<h1><%= @genre.name %></h1>

<h2>
  <ul>
    <% @genre.artists.each do |artist| %>
    <li><%= artist.name %></li>
    <% end %>
  </ul>
</h2>

<h2>
  <ul>
    <% @genre.songs.each do |song| %>
    <li><%= song.name %></li>
    <% end %>
  </ul>
</h2>
